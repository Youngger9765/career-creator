# Supabase 即時同步功能設定指南

## 🚀 快速開始

### 1. 建立 Supabase 專案

1. 前往 [Supabase Dashboard](https://app.supabase.com/)
2. 點擊「New Project」
3. 填寫專案資訊：
   - Project name: `career-creator` (或你喜歡的名稱)
   - Database Password: 設定一個強密碼
   - Region: 選擇 `Southeast Asia (Singapore)` (離台灣最近)
4. 點擊「Create new project」

### 2. 取得 API 金鑰

專案建立完成後：

1. 前往 **Settings → API**
2. 複製以下資訊：
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

### 3. 設定環境變數

編輯 `frontend/.env.local`：

```bash
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=你的_Project_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的_anon_public_key
```

### 4. 啟用 Realtime

1. 在 Supabase Dashboard 前往 **Database → Replication**
2. 確認 Realtime 已啟用（通常預設啟用）

---

## ✨ 功能說明

### Phase 1: Header 在線狀態（已實作）

- **功能**: 即時顯示房間內的在線用戶
- **技術**: Supabase Presence（純記憶體，不寫資料庫）
- **延遲**: <1 秒

使用方式：

1. 進入房間自動連接
2. Header 顯示在線用戶列表
3. 用戶離開自動消失（60秒內）

### Phase 2: 遊戲模式同步（待實作）

- **功能**: Owner 切換遊戲模式，所有人同步
- **技術**: Broadcast + Database

### Phase 3: 牌卡移動同步（待實作）

- **功能**: 即時同步牌卡移動
- **技術**: Broadcast + Event Sourcing

---

## 🧪 測試方式

### 單用戶測試

1. 設定好環境變數
2. 啟動前端：`cd frontend && npm run dev`
3. 進入任一房間
4. 檢查 Console 是否有「已加入房間 Presence」訊息
5. Header 應顯示你自己在線上

### 多用戶測試

1. 開啟兩個瀏覽器（或無痕模式）
2. **瀏覽器 A**: 以諮詢師身份登入，進入房間
3. **瀏覽器 B**: 以訪客身份（用分享碼）進入同一房間
4. 兩邊的 Header 都應該顯示兩個在線用戶
5. 關閉其中一個瀏覽器，60秒內對方的列表會更新

---

## 🔍 除錯指引

### 問題：看不到在線用戶

檢查事項：

1. 環境變數是否正確設定
2. Console 是否有錯誤訊息
3. Network 面板是否有 WebSocket 連線

### 問題：Supabase 未配置警告

```
⚠️ Supabase 環境變數未設定！
```

解決：確認 `.env.local` 有正確的 Supabase URL 和 Key

### 問題：連線失敗

可能原因：

- Supabase 專案尚未建立
- API Key 錯誤
- 網路問題

---

## 📊 免費版限制

- **Realtime 連線**: 200 個同時連線
- **訊息量**: 2M messages/月
- **傳輸量**: 2GB/月
- **資料庫**: 500MB

對於開發和初期使用完全足夠！

---

## 🎯 下一步

1. **現在**: 測試 Presence 功能是否正常
2. **之後**: 實作 Phase 2 (遊戲模式同步)
3. **最後**: 實作 Phase 3 (牌卡同步)

---

## 📚 相關資源

- [Supabase Realtime 文檔](https://supabase.com/docs/guides/realtime)
- [Presence 使用指南](https://supabase.com/docs/guides/realtime/presence)
- [專案實作文檔](./SUPABASE_SYNC_IMPLEMENTATION.md)

---

*最後更新: 2025-09-29*
